# 建模总纲报告

状态：未运行 `2analysis-modeling`。

本文件用于记录赛题分析、子问题拆解、候选模型比较、主线总纲、逐问报告索引和跨问题一致性检查。每一问的局部模型、推算过程、变量、假设、目标函数、约束、求解方法和代码实现任务清单，应写入 `reports/questions/Q*_MODELING.md`。

## 推荐推进方式

- 阶段1先写候选模型比较，说明 2-3 套方案的适用问题、优缺点、风险和实现难度。
- 阶段2先写主线总纲，明确主线模型族、共用假设、各问依赖、通用变量、指标口径、逐问顺序和 `reports/questions/Q*_MODELING.md` 文件清单。
- 阶段3开始逐问补细节，每一问进入代码前都要在独立的 `reports/questions/Q*_MODELING.md` 中补齐推算过程、局部模型、输入输出、校验方式和代码任务。

## 逐问报告索引

| 小问 | 报告 | 状态 |
| --- | --- | --- |
| 问题一 | `reports/questions/Q1_MODELING.md` | 待补充 |

## 格式要求

- 数学符号和公式必须使用标准 Markdown/LaTeX 数学格式。
- 行内公式使用 `$...$`，例如 `$s_i(t)$`、`$\mathbf{P}_i(t)=\gamma(s_i(t))$`。
- 行间公式使用 `$$...$$`。
- 不要用标记为 `text` 的代码块、反引号代码样式或纯文本伪公式表达数学公式。
- 等式统一用普通等号 `=`，不要用 `:=`。
- 优先使用直观符号；涉及对时间的变化率时，优先写成 `$\frac{d\theta(t)}{dt}$` 这类形式，不默认使用点号导数等简写。
- 公式要写成能渲染的完整表达，关系式必须有清楚的等号或不等号。

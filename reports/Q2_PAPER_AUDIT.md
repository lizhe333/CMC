# 问题二最终论文与版式独立审计

审计日期：2026-09-11  
审计范围：问题二正文、模型准备接口、摘要与模型评价中的问题二表述、问题二表图资产、LaTeX 编译与 PDF 第 13--17 页。  
变更边界：本轮未修改论文、代码或数值结果，只新增本审计报告；编译仅更新 LaTeX 构建产物。

## 1. 最终门禁

结论：**PASS**

- P1：无。
- P2：无。
- P3：无未关闭项。上一轮的页码、灵敏度方向和截断控制面下标范围三项 P3 已定点修复并通过最终复核。

问题二的数值、模型、表图与 PDF 版式均通过，**问题二论文同步完成并授权纳入最终论文**。整篇论文仍处于草稿态，非 Q2 的占位内容尚未闭合，不能据本报告宣称全稿可提交。

## 2. 审计依据与方法

本轮完整读取并按其门禁执行：

- `MathModelAgent/skills/5writing/SKILL.md`；
- `MathModelAgent/skills/6verity/SKILL.md`；
- 两项技能直接引用的 `MathModelAgent/skills/_references/math_modeling_norms.md`、`docs/WORKFLOW_CHECKLISTS.md`；
- `PAPER_WRITING_GATE.md`、`reports/PAPER_STYLE_GUIDE.md`；
- `reports/Q2_PAPER_VERIFY_REPORT.md`、`reports/Q2_RESULT_AUDIT.md`、`reports/Q2_RESULTS_REPORT.md`、`reports/RESULTS_REPORT.md`、`reports/questions/Q2_MODELING.md`、`reports/MODEL_AUDIT_REPORT.md`；
- `paper/main.tex`、`paper/sections/model_preparation.tex`、`paper/sections/6_q2_full_drying.tex`、`paper/sections/10_evaluation.tex`、`paper/tables/q2_*.tex`；
- `code/generate_q2_paper_assets.py`、`results/q2/q2_fields.npz`、两份论文 CSV、`q2_validation.json`、`q2_sensitivity.csv` 和问题二图形 PDF。

本审计没有沿用 `Q2_PAPER_VERIFY_REPORT.md` 的通过声明，而是重新做数组到 CSV/LaTeX 的逐项比较、图形文件同一性检查、强制编译、最终日志扫描、PDF 文本分页面定位和第 13--17 页视觉检查。

## 3. 数值与资产溯源

| 检查项 | 独立证据 | 结论 |
| --- | --- | --- |
| 验证状态 | `q2_validation.json` 为 `NUMERICAL_CHECKS_PASSED` | PASS |
| 原始场 | NPZ 中温度、含水率数组均为 `(10801,21)` | PASS |
| 论文取样 | 时刻严格为 1800、3600、5400、7200、9000、10800 s；半径严格为 0、0.5、1、1.5、2 cm | PASS |
| CSV 对 NPZ | 温度和含水率各 30 个值，最大绝对差均为 0 | PASS |
| LaTeX 对 CSV | 两张表均为 `6×5`；60 个数值逐项等于 CSV 四舍五入到四位小数的结果 | PASS |
| 论文图对结果图 | `results/q2/q2_fields.pdf` 与 `paper/figures/q2_fields.pdf` 的 SHA-256 同为 `47A3C075DBF568DBF0DE57BB39CAA2ED652EE951B9E9B63367FBDF32CF2D6516` | PASS |
| 生成器输入边界 | `generate_q2_paper_assets.py:18-19` 固定六时刻与五位置，`:77-88` 验证状态和表形状，未导入求解器 | PASS |

论文关键值与当前结果链一致：3 h 中心/表面温度为 `49.8495/49.9664 °C`，含水率为 `1.7662/1.0081 kg/kg`；空间加密最大差为 `3.426781e-6 °C`、`3.731918e-6 kg/kg`，时间加密最大差为 `3.751870e-6 °C`、`4.548273e-8 kg/kg`。摘要、正文和模型评价中的问题二数值没有发现冲突。

正文给出的扩散系数量级也可复算：初始、3 h 中心、3 h 表面分别为 `5.64168037e-9`、`1.23872591e-8`、`1.02717251e-8 m^2/s`，与正文的三位有效数字一致。

## 4. 方程、初边条件与数值方法一致性

### 4.1 连续模型

`paper/sections/6_q2_full_drying.tex:14-37` 的四项变物性、摄氏度到开尔文的转换和两条圆柱 PDE 与已审计模型一致：

- `T_K=T+273.15` 只进入扩散系数指数；
- `k(C)`、`D(C,T)` 均保留在圆柱散度算子内部；
- 温度方程使用题面口径下的 `rho(C)c_p(C)` 有效热传导模型；
- 正文在 `:70-72` 明确没有蒸发潜热，未把它表述为含相变焓的完整能量模型。

`paper/sections/6_q2_full_drying.tex:43-56` 的均匀初值、圆心对称条件和表面 Robin 条件与数值审计一致。表面条件的负号方向正确；固定半径、无收缩、无额外交叉源项等边界均在问题分析中显式说明。

### 4.2 离散与正性参数化

`paper/sections/6_q2_full_drying.tex:74-107` 正确描述了圆柱截断控制体、相邻界面算术平均、内部守恒通量、Robin 外边界通量、联立 BDF 和 `z=log C` 正性参数化。正文说明 `C=exp(z)` 后回到物理方程，输出仍为物理含水率且无裁剪；这与数值审计中验证的 `z_t=C_t/C` 严格等价关系一致。

解析 Jacobian 的表述没有超出已有证据：正文仅声称保留相邻热湿交叉依赖及交错偏移 `±1,±2,±3`，与 `Q2_RESULT_AUDIT.md` 的逐列中心差分和稀疏掩码检查一致。收敛差异也被表述为网格/时间加密的经验差异，没有冒充解析误差界。

最终复核确认式 (7.5) 已在 `paper/sections/6_q2_full_drying.tex:79-84` 写成分行定义：普通界面公式明确限定为 `i=0,...,N-1`，表面 `r_{N+1/2}=R` 作为截断后的特殊定义，形式与实现一致。

## 5. 写作结构、图表论证与结论边界

问题二正文具有四个二级小节：问题分析、模型建立、模型求解、结果分析，源文件位置分别为 `:3`、`:11`、`:74`、`:109`。论证顺序是“题意与缺参边界 -> 物性/PDE/定解条件 -> 守恒离散与可靠性 -> 表图结果、机理和续算边界”，符合已确认的优秀论文式组织。

- 温度表在 `:111-115` 被引出，表后立即解释中心--表面温差及其衰减；
- 含水率表在 `:117-125` 被引出，表后解释径向差、相对下降和不能用单点替代全场；
- 四联图在 `:127-134` 出现，左列包含 0.5、1、1.5、2、2.5、3 h 六个时刻，右列包含 0、0.5、1、1.5、2 cm 五个位置；
- 图后解释把温度推进、水分梯度和 `D(C,T)` 的两种竞争作用连接起来，没有只复述曲线；
- `:138` 明确前三小时结果不是烘干终点，并把三小时以后边界延拓和达标时刻交给问题三闭环。

摘要中问题二段落（`paper/main.tex:116`）与正文数值、方法和验证一致，没有使用“精度很高”“适用性强”等无证据结论。模型评价中的问题二条目（`paper/sections/10_evaluation.tex:21`）只概括 3 h 结果和同变量敏感性边界，没有越界推广。

灵敏度确为固定 `N=10240` 的单因素 `±20%`：改变 `h` 时 `h_m` 固定，改变 `h_m` 时 `h` 固定。由 CSV 独立复算，按扰动顺序 `-20%/+20%`，中心/表面含水率变化分别为：

- `h`：`+0.5883%/-0.3715%`、`+0.1504%/-0.1379%`；
- `h_m`：`+5.2073%/-4.1967%`、`+16.8006%/-13.3370%`。

最终正文在 `paper/sections/6_q2_full_drying.tex:139` 已分别写明“降低 20%”和“提高 20%”：`h_m` 降低时中心/表面增加 `5.21%/16.80%`、提高时减少 `4.20%/13.34%`；`h` 降低时增加 `0.59%/0.15%`、提高时减少 `0.37%/0.14%`。四组方向与 CSV 独立复算一致，且继续保留不跨摄氏度与 kg/kg 比较“强弱”的边界。

## 6. 字号、间距、编号与 PDF 视觉检查

源文件设置与版式要求一致：

- `paper/main.tex:2`：A4、正文 12 pt；
- `paper/main.tex:37-45`：一级标题 14 pt、二三级标题 12 pt；
- `paper/main.tex:139-143`：问题二局部 `linespread=1.20`；
- 两张问题二表的 `:3`：10.5 pt/12.6 pt；`:6`：宽度 `0.92\textwidth`；
- 公式编号、表号和图号经最终 `main.aux` 解析为式 (7.1)--(7.7)、表 5--6、图 4，引用均已定义。

使用 `latexmk -xelatex -interaction=nonstopmode -gg main.tex` 强制重建。实际执行 3 轮 XeLaTeX 后生成 `paper/main.pdf`：25 页，A4 `595.28×841.89 pt`，文件大小 3,415,919 字节。最终 `main.log` 定向扫描没有发现 `undefined`、`Overfull`、`Underfull`、`multiply defined`、`LaTeX Error` 或要求继续重跑引用的警告。

将 PDF 第 13--17 页以 170 dpi 渲染并逐页视觉检查：

| PDF 页 | Q2 内容 | 视觉结论 |
| ---: | --- | --- |
| 13 | Q1 收束后出现 Q2 标题和问题分析前半 | 标题不悬置；正文和前页衔接清楚，无裁切 |
| 14 | 问题分析后半、物性、PDE、初边条件 | 多行公式、编号和单位清晰，无越界或重叠 |
| 15 | 控制体/BDF/收敛、结果分析开头、表 5 | 表宽、字号、表头和六行数据清晰 |
| 16 | 表 6、结果解释、四联图及完整图题 | 两表与四面板均可读；六时刻含 2.5 h，五位置图例完整；图题未被页脚裁切 |
| 17 | 图后机理、灵敏度、工作簿边界，随后进入 Q3 | Q2 最后三段完整；与 Q3 分节清楚，无孤行或重叠 |

因此 Q2 的实际页码范围是 **第 13--17 页**，而不是既有报告所写的第 13--16 页。第 13 页与 Q1 共页，第 17 页与 Q3 共页；只数整页会漏掉 Q2 在第 17 页的三段结论。

## 7. 占位符、6verity 与全稿边界

`paper/sections/6_q2_full_drying.tex` 不含 `resultblank`、`draftnote` 或 `placeholder`。摘要的问题二段落和模型评价的问题二条目也不含占位。PDF 第 12--18 页文本层定向扫描及第 13--17 页视觉检查均未发现可见占位框、红色待确认块或占位图。

但全稿仍有以下非 Q2 未完成项，本报告不把它们误判为问题二失败：

- `paper/main.tex:65` 仍为 `draftmodetrue`；
- 摘要中的 Q1、Q4 和总验证结论仍有 `resultblank`（`main.tex:114,120,122`）；
- 模型评价的通用优点仍有 `draftnote`，Q1/Q4 结论仍有 `resultblank`（`10_evaluation.tex:7,20,23`）。

`6verity/scripts/writing_check.sh` 在 Git Bash 下运行到完成，退出码为 1。其 Q2 唯一 `FAIL` 是按 section 文件目录解析 `figures/q2_fields.pdf` 造成的路径误报；LaTeX 实际按 `main.tex` 目录成功加载该文件，且论文图与结果图哈希完全一致。其余提示是 Q1 嵌套输入文件、假设节偏短、备用流程图未引用和全稿无 citation marker，均属非 Q2 全稿事项。故脚本失败构成全稿预提交提醒，不构成 Q2 图形缺失证据。

## 8. 问题清单与可执行修复

### P1

无。

### P2

无。

### P3

无未关闭项。闭环证据如下：

| 上一轮 P3 | 最终证据 | 状态 |
| --- | --- | --- |
| 验证报告少计第 17 页 | `reports/Q2_PAPER_VERIFY_REPORT.md:61` 已改为第 13--17 页、五页，并说明第 17 页前三段属于 Q2 | CLOSED |
| 灵敏度方向顺序歧义 | `paper/sections/6_q2_full_drying.tex:139` 已逐项写明降低/提高 20%，与 `q2_sensitivity.csv` 独立复算一致 | CLOSED |
| 表面截断界面下标未限定 | 式 (7.5) 已限定 `i=0,...,N-1`，并单列两端截断界面 | CLOSED |

## 9. 提交与同步授权

最终门禁为 **PASS**。问题二的模型、数值、表图、摘要/评价表述和第 13--17 页版式已完成，可纳入最终论文；无需重跑问题二数值。最新 `main.pdf` 仍为 25 页 A4，式 (7.5) 保持在第 15 页、灵敏度段保持在第 17 页，视觉检查未发现回归；最终 `main.log` 对 undefined、overfull/underfull、multiply defined、缺字和 LaTeX/package error 的定向扫描均为 0。

全稿提交门禁仍须等待 Q1/Q4 占位、草稿开关、引用和其他非 Q2 全稿事项分别闭合。这些遗留不影响本次问题二 `PASS`，但在全稿验收前不能删除其风险记录。

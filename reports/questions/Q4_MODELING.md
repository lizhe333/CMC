# 问题四独立报告

状态：已建立本题专用骨架，等待附件 2 与前三问模型确认。

## 1. 题意复述

药材半径随失水缩小，需要在随时间变化的区域上重新计算含水率分布和首次全部达标时刻，并生成 `result4.xlsx`。

## 2. 已知条件与数据来源

- 附件 2 的半径时间序列。
- 问题二的热湿模型结构与问题三的终点判据。
- 附录 4 的状态相关物性公式。
- 附件 3 的 `result4.xlsx` 模板。

## 3. 推算过程

需要说明 $\xi=r/R(t)$ 坐标如何固定计算域，时间导数和径向导数如何变换，移动边界下体积积分和水分守恒如何保持一致，以及半径插值与 $\dot R(t)$ 如何稳定计算。

## 4. 局部模型

$$
\xi=\frac{r}{R(t)},\qquad 0\le\xi\le1.
$$

最终守恒方程须在模型审稿后确定，不能只把固定半径程序中的 $R_0$ 替换为 $R(t)$。

## 5. 控制变量对照

建立“固定/变化半径”与“附录 3/附录 4 物性”的 $2\times2$ 对照，区分几何收缩、物性公式变化及二者交互作用。

## 6. 代码接口

输入：附件 2、附录 4、阶段边界。输出：收缩域状态、烘干终点、四组对照、`result4.xlsx` 和守恒检查。

## 7. 结果与解释

状态：未运行代码。

## 8. 论文同步要点

对应 `paper/sections/8_q4_shrinkage.tex` 与 `paper/tables/q4_moisture.tex`。


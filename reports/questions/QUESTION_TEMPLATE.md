# 问题X：局部模型报告

## 0. 这一问要解决什么

用白话复述题目要求，说明本问输入是什么、要求输出什么、和前后问题有什么关系。

## 1. 已知条件与输出要求

| 类型 | 内容 |
| --- | --- |
| 已知条件 | ... |
| 输出文件 | ... |
| 论文展示 | ... |

## 2. 符号继承与本问新增符号

本问继承 `../SYMBOL_STYLE_GUIDE.md` 中的全局符号。若新增局部符号，只列本问真正需要的量。

| 符号 | 含义 | 说明 |
| --- | --- | --- |
| ... | ... | ... |

## 3. 推算过程

从题目条件开始逐步推导，不要直接给最终公式。

格式提醒：

- 数学表达统一使用 `$...$` 或 `$$...$$`。
- 等式统一用普通等号 `=`，不要用 `:=`。
- 优先使用直观符号；涉及对时间的变化率时，优先写成 `$\frac{d\theta(t)}{dt}$` 这类形式，不默认使用点号导数等简写。
- 公式要写成能渲染的完整表达，关系式必须有清楚的等号或不等号。

示例结构：

- 第一步：明确变量为什么这样定义。
- 第二步：说明约束或目标函数从题目哪句话来。
- 第三步：写出中间公式并解释每个符号。
- 第四步：说明公式如何转成算法或结果。

## 4. 局部模型

### 4.1 变量与符号

| 符号 | 含义 |
| --- | --- |
| $x$ | ... |

### 4.2 目标函数

$$
...
$$

### 4.3 约束条件

$$
...
$$

## 5. 求解方法

说明算法步骤、停止条件和可行性。

## 6. 代码任务

| 任务 | 输入 | 输出 | 校验 |
| --- | --- | --- | --- |
| ... | ... | ... | ... |

## 7. 结果校验

说明当前小问需要检查哪些数值、图表、约束和文件格式。

## 8. 模型审稿接口

进入代码前，交给 `2-5model-audit` 检查本问模型。这里列出审稿需要重点看的内容：

| 审稿维度 | 本问需要检查什么 |
| --- | --- |
| 正确性 | ... |
| 可行性 | ... |
| 新颖性 | ... |
| 全面性 | ... |

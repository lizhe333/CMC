# 问题二独立报告

状态：模型审计通过（PASS_WITH_FIXES），已进入问题二代码与前三小时结果阶段。本文记录实现所遵循的模型、输入输出契约和验收条件；数值结果以本问结果报告和验证 JSON 为准。

## 0. 本问解决什么问题

问题二要求把药材从开始烘干到前三小时的径向温度场和干基含水率场计算出来。与问题一相比，本问使用附录 3 的变物性经验公式：密度、比热容和导热系数随含水率变化，扩散系数同时随含水率和温度变化。因此温度场与含水率场需要在同一个非线性初边值问题中联立求解。

本问从 $t=0$ 重新开始计算，输出 $0$--$10800\,\mathrm{s}$ 的内部状态。问题一在 $1800\,\mathrm{s}$ 处的结果不作为本问初值，$1800\,\mathrm{s}$ 也不被人为设置为本问的阶段切换时刻。经用户选择并批准，本轮正式 result2.xlsx 固定保存 $t=1,\ldots,10800\,\mathrm{s}$ 的前三小时完整逐秒结果；这一口径满足前三小时交付，但题面“整个烘干过程”的措辞存在 Excel 是否应延伸到干燥终点的风险，需在问题三闭环后重新确认。问题二只使用附件 1 已覆盖的边界数据；超过该数据范围的长期延拓属于问题三的接口问题。

## 1. 题意、已知条件与边界

### 1.1 题面要求

药材近似为长 $25\,\mathrm{cm}$、半径 $2\,\mathrm{cm}$ 的圆柱。题面要求建立预热平衡与恒温干燥全过程的温度和水分浓度变化模型，按表 3 和表 4 给出 $3\,\mathrm{h}$ 内每隔 $0.5\,\mathrm{h}$、距离中心轴 $0$、$0.5$、$1$、$1.5$、$2\,\mathrm{cm}$ 处的结果，并将每隔 $1\,\mathrm{s}$、径向间隔 $0.1\,\mathrm{cm}$ 的完整结果保存到问题二结果文件。

这里的水分浓度沿用题面定义，是干基含水率，单位为 $\mathrm{kg/kg}$。它不是体积含水量，本文不把它额外乘以湿密度。水分方程采用题面尺度下的有效扩散模型。

### 1.2 已知数据与本问固定口径

| 类型 | 本问取值或处理 | 来源与说明 |
| --- | --- | --- |
| 几何尺寸 | 半径 $R=0.02\,\mathrm{m}$；长度 $L=0.25\,\mathrm{m}$ | 题面；一维径向方程中 $L$ 与 $2\pi L$ 在守恒式中约去 |
| 初始温度 | $T(r,0)=28\,^\circ\mathrm{C}$ | 题面 |
| 初始干基含水率 | $C(r,0)=2.55\,\mathrm{kg/kg}$ | 题面 |
| 环境数据 | 读取 compition/2026/附件1.xlsx 的第一张表，按时间、温度、水分浓度三列使用 | 附件 1；原始数据从 $0$ 到 $14400\,\mathrm{s}$，采样间隔为 $60\,\mathrm{s}$ |
| 环境插值 | 在相邻采样时刻之间对 $T_\infty(t)$、$C_\infty(t)$ 分别作分段线性插值 | 保留原始采样值，不对附件外时间隐式外推 |
| 换热系数 | 基准 $h=25\,\mathrm{W/(m^2\cdot K)}$ | 附录 2 标为问题一参数；问题二缺少新值，作为缺参下的延续假设，并对 $\pm20\%$ 做单因素灵敏度 |
| 传质系数 | 基准 $h_m=8\times10^{-7}\,\mathrm{m/s}$ | 附录 2 标为问题一参数；问题二缺少新值，作为缺参下的延续假设，并对 $\pm20\%$ 做单因素灵敏度 |
| 物性 | 全时段统一采用附录 3，不把问题一的常物性代入本问 | 题面明确要求问题二、三统一采用附录 3 |
| 当前计算时长 | 默认 $t_{\mathrm{end}}=10800\,\mathrm{s}$ | 对应题面要求的 $3\,\mathrm{h}$ |
| 额外物理项 | 不加入潜热、收缩、对流输运或未经题面支持的热湿交叉项 | 这是固定半径下的有效扩散简化 |

附件 1 的环境水分浓度数值与药材内部干基含水率使用题面同一表格口径，边界条件中直接使用给定的 $C_\infty(t)$。这表示一种有效传质边界，不额外声称它是由相对湿度换算得到的平衡含水率。

### 1.3 题面假设边界

本问采用以下可审计的简化：

1. 药材内部只保留径向变化，忽略端面传热、端面传质和轴向梯度。由于题面给出长径比为 $25/4$，这一近似用于固定半径的基准模型。
2. 半径在问题二中固定为 $R$，不使用附件 2 的收缩数据；尺寸变化留给问题四。
3. 传热方程使用局部热容量 $\rho(C)c_p(C)$，但不加入水分相变的显式潜热项。因此方程描述的是变物性有效热传导，而不是含蒸发焓的完整能量方程。
4. 水分变量 $C$ 直接作为有效扩散变量，使用 Fick 型守恒通量。干物质基准和固定几何使体积加权的 $C$ 守恒关系可以作为数值验收。
5. “预热平衡”和“恒温干燥”是题面过程描述。本实现对附件 1 的 $0$--$10800\,\mathrm{s}$ 数据使用一个连续的分段线性环境函数，不额外指定 $1800\,\mathrm{s}$ 的跳变或常值阶段。

最后一条是输出前三小时所采用的边界口径。若问题三需要继续到数十小时，必须另行明确附件 1 末端之后的边界延拓规则，求解器不得自动外推。

## 2. 符号继承与本问新增符号

本问继承 reports/SYMBOL_STYLE_GUIDE.md 和模型准备中的 $r,t,T,C,R,T_\infty,C_\infty,h,h_m$。其中 $T$ 在状态向量中统一以摄氏度存储；只有代入附录 3 的扩散系数时才转换为开尔文。

| 符号 | 含义 | 单位或范围 |
| --- | --- | --- |
| $R$ | 固定药材半径 | $\mathrm{m}$ |
| $T(r,t)$ | 药材温度 | $^\circ\mathrm{C}$ |
| $C(r,t)$ | 药材干基含水率 | $\mathrm{kg/kg}$ |
| $T_K(r,t)$ | 绝对温度，$T_K(r,t)=T(r,t)+273.15$ | $\mathrm{K}$ |
| $T_\infty(t),C_\infty(t)$ | 烘房环境温度和有效水分浓度 | $^\circ\mathrm{C}$、$\mathrm{kg/kg}$ |
| $r_i$ | 第 $i$ 个节点的径向坐标，$r_i=i\Delta r$ | $\mathrm{m}$ |
| $\Delta r$ | 空间步长，$\Delta r=R/N$ | $\mathrm{m}$ |
| $r_{i+1/2}$ | 相邻节点之间的控制面半径 | $\mathrm{m}$ |
| $V_i$ | 截断圆柱控制体的径向体积因子 | $\mathrm{m^2}$，不含 $2\pi L$ |
| $F^T_{i+1/2}$ | 热方程中的径向传导通量因子 | 与 $r k\,\partial T/\partial r$ 一致 |
| $F^C_{i+1/2}$ | 水分方程中的径向扩散通量因子 | 与 $r D\,\partial C/\partial r$ 一致 |
| $N$ | 径向区间数，必须为 $20$ 的倍数 | 正整数 |
| $y$ | 交错排列的联立状态向量 | $y=(T_0,C_0,T_1,C_1,\ldots,T_N,C_N)^\mathsf{T}$ |
| $\bar C(t)$ | 径向体积加权平均含水率 | $\mathrm{kg/kg}$ |
| $\mathcal L_C(t)$ | 单位长度累计表面水分外流量因子 | 与 $R h_m(C_s-C_\infty)$ 的时间积分一致 |

代码变量如 temperature_C、moisture_kg_per_kg、radius_m、boundary_temperature_C 与上述符号一一对应；不得用同一个代码变量同时表示摄氏度和开尔文。

## 3. 从题面到连续模型的推算

### STEP 1：确定径向状态变量

圆柱的长度方向和周向在本问中不展开，只保留中心轴到表面的径向坐标 $r\in[0,R]$。在任意固定位置，状态由温度 $T(r,t)$ 和干基含水率 $C(r,t)$ 给出。题面给出的初始状态均匀，因此

$$
T(r,0)=28,\qquad C(r,0)=2.55,\qquad 0\le r\le R.
$$

由于圆心是轴对称面，圆心没有径向通量：

$$
\left.\frac{\partial T}{\partial r}\right|_{r=0}=0,\qquad
\left.\frac{\partial C}{\partial r}\right|_{r=0}=0.
$$

### STEP 2：把附录 3 经验式写成局部物性

题面要求问题二和问题三统一使用附录 3。对每个位置的当前状态，定义

$$
\rho(C)=650+128C,
$$

$$
c_p(C)=1450+2736\frac{C}{C+1},
$$

$$
k(C)=0.21+0.38\frac{C}{C+1},
$$

$$
D(C,T)=2.4\times10^{-3}
\exp\left(-\frac{0.45}{C}\right)
\exp\left(-\frac{3850}{T_K}\right),
\qquad T_K=T+273.15.
$$

相应单位分别为 $\mathrm{kg/m^3}$、$\mathrm{J/(kg\cdot K)}$、$\mathrm{W/(m\cdot K)}$ 和 $\mathrm{m^2/s}$。在问题二的有效状态域中要求 $C>0$、$T_K>0$；代码遇到非正含水率或非有限物性时应停止并报告，而不是把状态静默截断到另一个模型。

### STEP 3：写出变物性热湿耦合方程

第一问的圆柱薄壳守恒关系只需把常物性替换为当前状态的局部物性，并保持系数在空间散度内。得到

$$
\rho(C)c_p(C)\frac{\partial T}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}
\left(rk(C)\frac{\partial T}{\partial r}\right),
\qquad 0<r<R,\ t>0,
$$

$$
\frac{\partial C}{\partial t}
=\frac{1}{r}\frac{\partial}{\partial r}
\left(rD(C,T)\frac{\partial C}{\partial r}\right),
\qquad 0<r<R,\ t>0.
$$

这里的双向耦合来自状态相关物性：

- $C$ 改变 $\rho(C)$、$c_p(C)$ 和 $k(C)$，从而改变局部热惯性和热传导；
- $C$ 和 $T$ 共同改变 $D(C,T)$，从而改变水分扩散；
- 温度变化会通过 $D(C,T)$ 反馈到含水率场，含水率变化又会通过 $\rho c_p$ 和 $k$ 反馈到温度场。

方程中不再添加一个形式上的 $T$--$C$ 交叉源项。附录 3 已经通过物性函数给出了题面允许的耦合机制；加入潜热或其他交叉项会改变问题口径，也缺乏题面参数支持。

### STEP 4：施加表面交换条件

记表面状态为 $T_s(t)=T(R,t)$、$C_s(t)=C(R,t)$。采用附录 2 的换热和传质系数，表面 Robin 条件为

$$
-k(C_s)\left.\frac{\partial T}{\partial r}\right|_{r=R}
=h\left[T_s(t)-T_\infty(t)\right],
$$

$$
-D(C_s,T_s)\left.\frac{\partial C}{\partial r}\right|_{r=R}
=h_m\left[C_s(t)-C_\infty(t)\right].
$$

左侧是以材料外法向为正的传导或扩散通量，右侧是表面与环境之间的交换通量。当 $T_s<T_\infty$ 时，热通量方向指向药材内部；当 $C_s>C_\infty$ 时，水分从药材流向环境。控制体离散时，表面关系被直接转化为边界通量，避免用一个虚假的外部节点代替环境。

### STEP 5：确定环境函数

设附件 1 的相邻采样点为 $(t_m,T_{\infty,m},C_{\infty,m})$ 和 $(t_{m+1},T_{\infty,m+1},C_{\infty,m+1})$。对 $t_m\le t\le t_{m+1}$，取

$$
\lambda(t)=\frac{t-t_m}{t_{m+1}-t_m},
$$

$$
T_\infty(t)=T_{\infty,m}
+\lambda(t)\left(T_{\infty,m+1}-T_{\infty,m}\right),
$$

$$
C_\infty(t)=C_{\infty,m}
+\lambda(t)\left(C_{\infty,m+1}-C_{\infty,m}\right).
$$

问题二的正式计算只取 $0\le t\le10800\,\mathrm{s}$，因此完全落在附件 1 的覆盖范围内。读取器必须检查时间严格递增、数据有限、无缺失，并在请求时间超出原始数据末端时拒绝求解。

## 4. 可编码的控制体离散

### 4.1 径向网格与控制体

取 $N$ 个径向区间，节点为

$$
r_i=i\Delta r,\qquad \Delta r=\frac{R}{N},\qquad i=0,1,\ldots,N.
$$

为了让 $0.1\,\mathrm{cm}$ 的题面输出位置落在节点上，$N$ 必须是 $20$ 的倍数。控制面取

$$
r_{i+1/2}=\left(i+\frac12\right)\Delta r,
$$

并在圆心和表面截断。令 $r_{-1/2}=0$、$r_{N+1/2}=R$，第 $i$ 个控制体的径向体积因子为

$$
V_i=\frac{r_{i+1/2}^2-r_{i-1/2}^2}{2},
\qquad i=0,1,\ldots,N.
$$

实际控制体体积为 $2\pi L V_i$。由于每一项都含有该因子，本问的离散方程可约去 $2\pi L$，但不能把圆柱权重 $V_i$ 换成相同的笛卡尔网格权重。

### 4.2 节点和界面物性

在节点 $i$ 上根据当前状态计算

$$
\rho_i=\rho(C_i),\qquad
c_{p,i}=c_p(C_i),\qquad
k_i=k(C_i),\qquad
D_i=D(C_i,T_i).
$$

相邻节点之间的界面物性按本问固定的算术平均处理：

$$
k_{i+1/2}=\frac{k_i+k_{i+1}}{2},\qquad
D_{i+1/2}=\frac{D_i+D_{i+1}}{2}.
$$

算术平均只用于界面通量，不得把 $k(C)$ 或 $D(C,T)$ 从连续方程的散度算子外提出。

### 4.3 保守通量与半离散方程

定义内部界面通量因子

$$
F^T_{i+1/2}
=r_{i+1/2}k_{i+1/2}
\frac{T_{i+1}-T_i}{\Delta r},
$$

$$
F^C_{i+1/2}
=r_{i+1/2}D_{i+1/2}
\frac{C_{i+1}-C_i}{\Delta r},
\qquad i=0,1,\ldots,N-1.
$$

它们分别对应 $r k\,\partial T/\partial r$ 和 $rD\,\partial C/\partial r$，因此控制体守恒式为

$$
\rho_i c_{p,i}V_i\frac{\mathrm{d}T_i}{\mathrm{d}t}
=F^T_{i+1/2}-F^T_{i-1/2},
$$

$$
V_i\frac{\mathrm{d}C_i}{\mathrm{d}t}
=F^C_{i+1/2}-F^C_{i-1/2}.
$$

圆心条件给出

$$
F^T_{-1/2}=0,\qquad F^C_{-1/2}=0.
$$

表面 Robin 条件给出外控制面的通量

$$
F^T_{N+1/2}
=-R h\left[T_N-T_\infty(t)\right],
$$

$$
F^C_{N+1/2}
=-R h_m\left[C_N-C_\infty(t)\right].
$$

因此在表面控制体中直接使用 $F_{N+1/2}$，而不是把表面节点错误地当成一个完整内部控制体。$k_N$ 和 $D_N$ 仍由当前表面状态计算，用于 Robin 条件对应的表面梯度；将 Robin 条件化为通量后，外控制面通量显式由 $h$ 或 $h_m$ 给出。

### 4.4 联立状态与非线性求解

将每个节点的两个状态交错排列为

$$
y(t)=\left(T_0,C_0,T_1,C_1,\ldots,T_N,C_N\right)^\mathsf{T}.
$$

每次评价右端时按以下顺序更新：

1. 从 $y$ 取出 $T_i$ 和 $C_i$，并检查 $C_i>0$、$T_i+273.15>0$；
2. 计算所有节点物性和界面物性；
3. 计算内部界面通量以及两个表面交换通量；
4. 用控制体方程得到各节点的 $\mathrm{d}T_i/\mathrm{d}t$ 和 $\mathrm{d}C_i/\mathrm{d}t$；
5. 按交错顺序返回联立右端。

采用 BDF 隐式积分。每个热方程行依赖节点 $i-1,i,i+1$ 的 $T,C$，每个水分方程行也依赖这些节点的 $T,C$，因为 $D(C,T)$ 同时含有温度和含水率。因此 Jacobian 使用以节点为块的三对角稀疏结构；不能把热块和湿块假设成两个互不相交的矩阵。

为构造解析 Jacobian 或核对自动差分，附录 3 公式的局部导数为

$$
\frac{\mathrm{d}\rho}{\mathrm{d}C}=128,
\qquad
\frac{\mathrm{d}c_p}{\mathrm{d}C}=\frac{2736}{(C+1)^2},
\qquad
\frac{\mathrm{d}k}{\mathrm{d}C}=\frac{0.38}{(C+1)^2},
$$

$$
\frac{\partial D}{\partial C}
=D\frac{0.45}{C^2},
\qquad
\frac{\partial D}{\partial T}
=D\frac{3850}{(T+273.15)^2}.
$$

这里 $\partial D/\partial T$ 的温度变量仍是摄氏度数值，因为 $T_K=T+273.15$ 的导数为 $1$。如果实现暂不提供完整解析 Jacobian，至少提供上述三对角块稀疏结构给 BDF，并用有界差分验证热湿交叉块确实存在。

## 5. 求解接口与输出契约

### 5.1 求解器输入

后续实现新增独立文件 code/q2_solver.py，不改写问题一求解器。建议接口如下：

| 参数 | 默认值或要求 | 校验 |
| --- | --- | --- |
| boundary | 附件 1 路径 | 必须含时间、温度、水分浓度三列 |
| end | $10800\,\mathrm{s}$ | 必须为正整数，且不超过边界原始数据末端 |
| n 或网格递进参数 | 初始 $N=20$ | $N$ 为 $20$ 的倍数 |
| rtol | $10^{-9}$ | 大于零 |
| atol | $10^{-11}$ | 大于零 |
| max_step | $5\,\mathrm{s}$ | 大于零；时间精度检查时减半 |
| template | 附件 3 的 result2.xlsx | 只读载入，不能直接覆盖 |
| output | 问题二结果目录 | 保存未舍入结果和验证记录 |

read_boundary 必须拒绝重复或倒序时间、缺失值、非有限值、非法温度和请求区间不被覆盖的情况。任何附件末端之后的时间都必须显式给出新的边界函数，不能调用插值函数产生静默端点外推。正式 result2.xlsx 先以原始模板为底稿生成候选文件并逐格回读，验收通过后覆盖用户指定正式路径；候选文件是过程产物，不替代正式交付。

### 5.2 求解与存储流程

1. 读取环境数据并保存实际使用的边界副本，确认请求区间为 $0$--$10800\,\mathrm{s}$。
2. 按 $T_i(0)=28$、$C_i(0)=2.55$ 初始化全部节点；不读取问题一的末状态。
3. 用联立 BDF 在内部自适应时间步上积分，输出点固定为每个整数秒；输出采样不能反过来限制非线性积分的内部步长。
4. 在每个整数秒提取 $r=0,0.1,\ldots,2.0\,\mathrm{cm}$ 的 21 个节点值。保留 $t=0$ 的数组用于初值和连续性核对。
5. 单独生成论文表：时间为 $1800,3600,5400,7200,9000,10800\,\mathrm{s}$，即 $0.5$--$3.0\,\mathrm{h}$；径向位置为 $0,0.5,1,1.5,2\,\mathrm{cm}$。
6. 生成候选工作簿后逐格回读，确认工作表、时间、距离和四位小数均与未舍入数组一致，再把通过验收的候选文件复制为正式 result2.xlsx。原始模板保留不改。

内部未舍入数组的形状为两组 $(10801,21)$，覆盖 $t=0,1,\ldots,10800\,\mathrm{s}$。正式工作簿固定填入 $t=1,\ldots,10800\,\mathrm{s}$，共 $10800$ 个数据行；$t=0$ 保存在未舍入结果和诊断文件中。工作簿的行数、时间列和内部数组的索引关系必须在导出验收中逐项核对，不能在文件之间混用两种行数定义。

### 5.3 预期输出文件

| 输出 | 内容 |
| --- | --- |
| q2_fields.npz | 未舍入温度、含水率、整数秒时间、21 个径向位置和必要诊断 |
| q2_restart.npz | $N+1$ 个节点的末时刻全网格温度、含水率、节点坐标、控制体权重、末时刻、$N$、$h$、$h_m$ 与边界配置 |
| q2_temperature_full.csv、q2_moisture_full.csv | 含 $t=0$ 的完整逐秒数组 |
| boundary_used.csv | 求解器实际使用的逐秒环境边界 |
| q2_temperature_paper.csv、q2_moisture_paper.csv | 表 3、表 4 的 60 个未舍入取值 |
| result2.xlsx | “温度”和“水分浓度”两个工作表，数值显示四位小数 |
| q2_validation.json | 输入路径、物性口径、网格、容差、边界覆盖、收敛和验收状态 |
| Q2_RESULTS_REPORT.md | 结果来源、数值检查、表格与图形来源、复现命令 |

工作簿的第一行保留“时间（s）与距离（cm）”字段和 $0$--$2\,\mathrm{cm}$、间隔 $0.1\,\mathrm{cm}$ 的距离表头；温度和水分浓度分别写入原模板的两个工作表。数值可以按四位小数显示，但验收必须用未舍入数组的四舍五入结果逐格比较。

## 6. 数值验收与模型审计

### 6.1 物性和耦合检查

主 agent 审计时至少应独立核对：

- 用 $C=2.55$、$T=28^\circ\mathrm{C}$ 代入四个附录 3 公式，检查单位、数量级和 $T+273.15$ 的开尔文转换；
- 随机选取一个非均匀状态，确认每个节点都更新 $\rho,c_p,k,D$，界面使用相邻节点平均；
- 检查水分方程对邻接温度的依赖以及热方程对邻接含水率的依赖，确认 Jacobian 稀疏结构没有切断交叉块；
- 检查 $C\le0$、$T+273.15\le0$、非有限物性时是否明确失败，不能用无说明的裁剪掩盖模型失效。

### 6.2 离散和边界检查

需要使用针对性合成场景核对离散式：

1. 在 $T_\infty=T_0$、$C_\infty=C_0$ 且初场均匀的平衡测试中，热湿状态保持不变；
2. 对常物性极限场景，检查圆心截断控制体、内部界面通量和表面 Robin 通量与圆柱导热解析解的收敛方向一致；
3. 对非均匀 $T,C$ 场检查相邻控制体界面通量一正一负，确认内部通量不会凭空产生或损失；
4. 检查实际数据下温度和含水率状态为有限值，含水率不出现明显负值，温度不超过环境与初始值构成的最大值范围之外。

合成测试只用于验证方程和代码，不得把合成场景的数值混入问题二结果表。

### 6.3 网格和时间收敛

空间网格按 $N=20,40,80,\ldots$ 逐级加密，至少比较相邻网格在全部整数秒、21 个输出位置上的最大绝对差异。温度和含水率使用分量化的有单位误差：

$$
e_T=\max\left|T^{(N)}-T^{(2N)}\right|
<5\times10^{-5}\,^\circ\mathrm{C},
\qquad
e_C=\max\left|C^{(N)}-C^{(2N)}\right|
<5\times10^{-5}\,\mathrm{kg/kg}.
$$

并且两张论文表在最后两级网格上四位小数完全一致。网格最多递进到 $N=10240$；若仍未通过，保存诊断并标记为需要回修，不能把未收敛结果写入正式工作簿或论文。

时间检查以 $\mathrm{rtol}=10^{-9}$、$\mathrm{atol}=10^{-11}$、最大步长 $5\,\mathrm{s}$ 为基准，再将两种容差同时收紧十倍并将最大步长减半。分别记录 $e_T^{\mathrm{time}}$ 和 $e_C^{\mathrm{time}}$，二者都必须低于 $5\times10^{-5}$，并检查论文表四位小数和积分器成功状态。四位小数一致只能作为交付稳定性证据，不能宣称为严格误差界。

### 6.4 含水率守恒检查

由半离散水分方程求和，内部界面通量相互抵消，得到

$$
\frac{\mathrm{d}}{\mathrm{d}t}
\left(\sum_{i=0}^{N}V_iC_i\right)
=-R h_m\left[C_N-C_\infty(t)\right].
$$

定义单位长度累计外流量因子

$$
\mathcal L_C(t)
=\int_0^t R h_m\left[C_s(\tau)-C_\infty(\tau)\right]\mathrm{d}\tau,
$$

则应满足

$$
\bar C(t)+\frac{\mathcal L_C(t)}{R^2/2}
=\bar C(0),
\qquad
\bar C(t)=
\frac{\sum_{i=0}^{N}V_iC_i}{R^2/2}.
$$

报告中将其命名为“有效含水率方程的离散通量闭合检查”，并给出最大残差和计算方法。由于 $C$ 是有效干基扩散变量，该关系是本模型的代数数值检查；不要把它写成湿物料总质量守恒的实验验证。

### 6.5 热量诊断的限制

对每一时刻，可以用

$$
\sum_{i=0}^{N}\rho_i c_{p,i}V_i\frac{\mathrm{d}T_i}{\mathrm{d}t}
-F^T_{N+1/2}
$$

检查离散热方程与表面热通量的瞬时平衡。由于 $\rho$ 和 $c_p$ 会随 $C$ 变化，而模型没有加入含水率变化引起的显式焓项，简单计算 $\sum_i\rho_i c_{p,i}V_iT_i$ 的变化不能被称为严格总能量守恒。结果报告只把它作为离散方程和边界通量诊断，并明确这一物理简化。

### 6.6 Excel 和论文值验收

正式文件验收必须包括：

- 工作表名称恰为“温度”和“水分浓度”；
- 时间列严格为 $1,2,\ldots,10800$，距离列严格为 $0,0.1,\ldots,2.0\,\mathrm{cm}$；
- 每一格均可回读且为有限数，四位小数与未舍入数组 round 后的结果完全一致；
- 表 3、表 4 的 60 个值与同一数组按时间和位置索引得到的值一致；
- 图形若生成，必须直接读取同一份未舍入数组，不得手填或另行模拟。

### 6.7 缺参延续假设的灵敏度

由于问题二没有重新给出 $h$ 和 $h_m$，基准值只是沿用附录 2 的缺参延续假设。基准计算通过后，固定所有其他参数和最终收敛网格，分别运行

$$
h\in\{0.8h_0,1.2h_0\},\qquad h_m=h_{m,0},
$$

以及

$$
h=h_0,\qquad h_m\in\{0.8h_{m,0},1.2h_{m,0}\}.
$$

灵敏度文件至少记录 $3\,\mathrm{h}$ 时中心和表面的 $T,C$，以及相对于基准的差值；不能把灵敏度场景替换为正式基准结果。

## 7. 问题三的接口

问题三判定首次达到

$$
\max_{0\le r\le R}C(r,t)<0.15
$$

的时刻，因此需要复用本问的热湿联立右端、物性函数、控制体网格和状态格式。问题二实现应保留以下 continuation 接口：

| 接口量 | 问题二交付 | 问题三用途 |
| --- | --- | --- |
| 最后时刻内部全网格状态 | $T_i(10800)$、$C_i(10800)$，不只保存 21 个展示点 | 作为继续积分的状态 |
| 网格与控制体权重 | $N,\Delta r,V_i$ | 保证阈值判断和续算使用同一空间离散 |
| 物性和边界配置 | 附录 3、$h$、$h_m$、边界插值方式 | 保持跨问口径一致 |
| 边界时间接口 | 已覆盖区间内分段线性函数 | 在附件末端之后接入经审计的恒温恒湿或其他明确规则 |
| 事件判定 | 输出全空间 $C_i$，并保留相邻时间状态 | 用线性或受控插值定位阈值穿越，不能用平均含水率代替最大值 |

问题三开始前必须书面确定 $t>14400\,\mathrm{s}$ 的环境延拓。当前报告只规定“超出原始附件范围拒绝隐式外推”，不预先生成问题三的达标时刻。问题四还会改变半径和附录 4 物性，不能直接把本问固定域的结果当成问题四结果。

## 8. 论文同步要点

后续论文小节 paper/sections/6_q2_full_drying.tex 应按以下顺序组织：

1. 说明第一问提供圆柱径向框架，本问从 $t=0$ 重新采用附录 3；
2. 集中给出 $\rho(C)$、$c_p(C)$、$k(C)$、$D(C,T)$，明确 $T$ 在指数中取 K；
3. 给出热方程、水分方程、初边值条件和控制体通量，解释耦合来自状态相关物性；
4. 插入表 3、表 4，并用实际结果解释中心、内部和表面响应差异；
5. 说明无潜热、无收缩和边界有效浓度口径，避免把模型简化写成实验事实；
6. 由 q2_validation.json、Q2_RESULTS_REPORT.md 和图形数据生成所有数值，不在正文手工输入未溯源数字。

本阶段不改论文文件，不填充表格占位符，也不写入问题二数值结论。

## 9. 交给 2-5model-audit 的重点

| 审稿维度 | 需要核对的具体问题 |
| --- | --- |
| 正确性 | 附录 3 指数中的温度是否为 K；$k,D$ 是否留在散度通量内；Robin 边界符号和圆心控制体是否正确；干基含水率守恒是否使用圆柱权重 |
| 可行性 | BDF 联立求解是否能处理早期表面梯度；$N$ 递进和时间容差是否可在现有环境完成；结果文件是否能逐格回读 |
| 新颖性 | 只把题面给出的状态相关物性作为耦合来源，不把常规变系数 PDE 夸大为额外创新 |
| 全面性 | 是否明确从 $t=0$ 重算、无 $1800\,\mathrm{s}$ 人工切换、边界不外推、无潜热和问题三续算接口；是否区分数值误差与物理简化误差 |

通过审计后，子 agent 才进入 code/q2_solver.py、结果计算和论文同步阶段。


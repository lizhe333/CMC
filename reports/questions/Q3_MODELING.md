# 问题三独立报告

状态：已建立本题专用骨架，等待问题二求解器确认。

## 1. 题意复述

持续运行问题二模型，找到当前药材区域内所有位置的干基含水率首次都低于 0.15 kg/kg 的时刻，并输出每 60 s、每 0.1 cm 的完整结果。

## 2. 已知条件与数据来源

- 问题二的全过程温度场和含水率场。
- 题面阈值 0.15 kg/kg。
- 附件 3 的 `result3.xlsx` 模板。

## 3. 推算过程

定义空间最大含水率，先包围首次阈值穿越区间，再用事件检测、二分或局部插值细化终点；给出终点前后证据。

## 4. 局部模型

$$
C_{\max}(t)=\max_{0\le r\le R_0}C(r,t),\qquad
t_{\mathrm{end}}=\inf\{t>0:C_{\max}(t)<0.15\}.
$$

## 5. 代码接口

输入：问题二状态推进器。输出：终点、终点控制节点、前后最大含水率、`result3.xlsx` 和终点局部放大数据。

## 6. 结果与解释

状态：未运行代码。

## 7. 对后续问题的影响

本问结果作为问题四固定半径且采用附录 3 物性的基准方案。

## 8. 论文同步要点

对应 `paper/sections/7_q3_end_time.tex` 与 `paper/tables/q3_moisture.tex`。


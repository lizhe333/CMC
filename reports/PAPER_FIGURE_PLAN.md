# 2026A 模型准备及第一问图表安排

更新：2026-09-10。按用户最新意见直接维护原 LaTeX 工程。沿用题目数据和已有结果，不重新求解。

## 图的位置

1. **四问建模流程**：放在问题分析的整体技术路线处，用项目 `figures/流程图.svg` 替换原框架示意图。为 XeLaTeX 转为矢量 PDF，保存为 `paper/figures/model_flowchart.pdf`，保留 `fig:framework` 标签。
2. **圆柱药材的径向传递及薄壳微元**：放在模型准备的薄壳能量守恒推导前。沿用项目 `figures/一维圆柱体非线性热传导示意图.png`，原样复制到 `paper/figures/cylinder_heat_transfer.png`，标签为 `fig:cylinder-shell`。该图用于说明几何、交换方向及定性径向差异，不作为数值结果；第一问常热物性下的导热方程仍为线性方程。
3. **预热阶段温度与含水率的径向分布及时间变化**：沿用 `paper/figures/q1_fields.pdf`，置于第一问两张结果表及相应解释之后，标签为 `fig:q1-fields`。曲线及数据保持原样。

所有图号由 LaTeX 自动生成，正文在邻近位置引图并解释。

## 表和公式

各问关联与数值检验改用正文，不设表格。题目参数用不编号的大括号关系表达。仅保留两张题目规定的温度和含水率结果表，每张含 7 个时刻、5 个半径的结果；小数位、宽度、行高统一。

第一问分为“药材热传导”和“水分蒸发”两组初边值问题，分别使用 `eq:q1-heat-model`、`eq:q1-moisture-model`。水分蒸发仍按内部有效扩散及表面传质描述，不增设相变源项。共用导热推导只在模型准备出现一次。

## 排版和验收

数学分式使用 `\frac`，物理单位使用斜杠。关键方法和结果适度加粗，图表前后紧接解释。

本轮 XeLaTeX 自动多轮编译通过，24 页 PDF 中无未解析引用，最终日志无溢出或缺字告警。已查看流程图、薄壳示意图、两组方程及原结果表图所在页。两张结果表共 70 个值与原 CSV 的四位小数表示一致。

交付覆盖 `output/pdf/CUMCM_2026_A_q1_results.pdf`。不另存版本，不生成压缩包。全稿未完成的小问与原有占位内容不在本轮定稿范围内。


## 附件一环境曲线（2026-09-11）

按确认方案在“2.2 数据预处理”中新增“烘房温度及水分浓度随时间的变化”，标签 `fig:ambient-boundary`。上下子图分别为温度、水分浓度，共用小时横轴，展示附件一全部 241 个采样点及逐秒分段线性插值。灰色区域标出第一问的前 30 分钟，不将其解释为已经达到热平衡。

图由 `code/plot_ambient_boundary.py` 直接读取原附件一生成，矢量版位于 `paper/figures/ambient_boundary.pdf`，预览位于 `figures/ambient_boundary.png`，数据口径记录在 `reports/AMBIENT_FIGURE_DATA.json`。无平滑、无区间外延拓，不写入既有求解结果。前 1800 秒与保存的 `results/q1/boundary_used.csv` 最大绝对差为 0。插图自动编号为图 1，其余图号顺延。
